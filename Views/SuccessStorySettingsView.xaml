<UserControl x:Class="SuccessStory.SuccessStorySettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="600">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </UserControl.Resources>
    
    <DockPanel Margin="20" Name="SuccessStorySettings">
        <StackPanel DockPanel.Dock="Top" Height="250">
            <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                <CheckBox IsChecked="{Binding enableSteam}" Width="280">
                    <Label Content="{DynamicResource LOCSucessStoryEnableSteam}"></Label>
                </CheckBox>
                <Label x:Name="SteamLoad" Width="90">xxx / xxx</Label>
                <Button Content="{DynamicResource LOCSucessStoryRefreshData}" Click="Button_Click_Steam"></Button>
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                <CheckBox IsChecked="{Binding enableGog}" Width="280">
                    <Label Content="{DynamicResource LOCSucessStoryEnableGog}"></Label>
                </CheckBox>
                <Label x:Name="GogLoad" Width="90">xxx / xxx</Label>
                <Button Content="{DynamicResource LOCSucessStoryRefreshData}" Click="Button_Click_Gog"></Button>
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                <CheckBox IsChecked="{Binding enableOrigin}" Width="280">
                    <Label Content="{DynamicResource LOCSucessStoryEnableOrigin}"></Label>
                </CheckBox>
                <Label x:Name="OriginLoad" Width="90">xxx / xxx</Label>
                <Button Content="{DynamicResource LOCSucessStoryRefreshData}" Click="Button_Click_Origin"></Button>
            </StackPanel>


            <StackPanel Orientation="Horizontal" Margin="0,0,0,15" Visibility="{Binding enableLocal, Converter={StaticResource BooleanToVisibilityConverter}}">
                <Label Content="Local Achievements" Width="255" Margin="25,0,0,0"></Label>
                <Label x:Name="LocalLoad" Width="90">xxx / xxx</Label>
                <Button Content="{DynamicResource LOCSucessStoryRefreshData}" Click="Button_Click_Local"></Button>
            </StackPanel>

            <StackPanel Margin="0,20,0,0">
                <ProgressBar Name="SuccessStoryLoad" Height="28" Margin="20,0"></ProgressBar>
            </StackPanel>
        </StackPanel>
       
        <StackPanel DockPanel.Dock="Bottom">
            <Button Content="{DynamicResource LOCSucessStoryRefreshAllDataRecent}" HorizontalAlignment="Center" VerticalAlignment="Center" 
                    Height="40" FontSize="18" Click="Button_Click_All_Recent" Margin="0,0,0,20"/>
            <Button Content="{DynamicResource LOCSucessStoryRefreshAllData}" HorizontalAlignment="Center" VerticalAlignment="Center" 
                    Height="40" FontSize="18" Click="Button_Click_All" />
        </StackPanel>
    </DockPanel>
</UserControl>